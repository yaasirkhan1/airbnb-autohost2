node_modules/
data/
!data/properties-map.json
!data/unit-profiles.json
.env
*.log

# generated script outputs (read-only data dumps)
scripts/price-pull.csv
scripts/properties-list.json

# real door codes — never commit (use ENTRY_CODES_JSON env on Railway)
config/entry-codes.json

# local-only Claude Code permission overrides
.claude/settings.local.json
